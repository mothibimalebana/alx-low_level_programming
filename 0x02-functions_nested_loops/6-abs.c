#include "main.h"
/**
 * _abs - A function that computes the absolute value of an integer.
 * @n: Takes parameter of type int (0 to 127)
 *
 * Return: Returns a value.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
